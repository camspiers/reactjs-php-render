{
	"name": "reactjs-php-render-tests",
	"description": "React for unit tests",
	"version": "0.0.0",
	"dependencies": {
		"react": "~0.10",
		"browserify": "~3.26.0"
	},
	"scripts": {
		"build": "NODE_ENV=production browserify -r react -r ./TestComponent > bundle.js"
	}
}